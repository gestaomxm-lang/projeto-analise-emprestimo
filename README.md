# 🏥 App de Análise de Empréstimos

Este projeto foi convertido em um aplicativo web interativo usando **Streamlit**.

## 🚀 Como rodar localmente

1. **Instale as dependências**:
   Abra o terminal na pasta do projeto e execute:
   ```bash
   pip install -r requirements.txt
   ```

2. **Execute o aplicativo**:
   ```bash
   streamlit run streamlit_app.py
   ```
   O navegador abrirá automaticamente com o app.

## 🌐 Como colocar na internet (Link Público)

Para que qualquer pessoa possa acessar com um link, a maneira mais fácil e gratuita é usar o **Streamlit Community Cloud**:

1. **Crie uma conta no GitHub** (se não tiver) e suba estes arquivos para um novo repositório (`streamlit_app.py` e `requirements.txt`).
2. **Crie uma conta no [Streamlit Cloud](https://streamlit.io/cloud)**.
3. Conecte seu GitHub e selecione o repositório que você criou.
4. Clique em **Deploy**.
5. 🎉 Pronto! Você receberá um link (ex: `https://seu-app.streamlit.app`) para compartilhar com qualquer pessoa.

## 📂 Arquivos
- `streamlit_app.py`: O código do aplicativo web.
- `analise_app_.py`: O script original (mantido para referência).
- `requirements.txt`: Lista de bibliotecas necessárias.
